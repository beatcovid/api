
def from_readable_dict(d):
    """
        Kobo has a format known as a "readable dict" where it
        flattens out keys for embedded dicts into name/name/name
        format. Here we unpack it.
    """
    pass
